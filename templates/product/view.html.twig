{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
    <style>
        body{
            background: #fff;
        }
        h1{
            display: flex;
            justify-content: center;
        }
    </style>
<div>
    <h1>{{ content }}</h1>

    <h2>Affichage du produit {{ id }}</h2>
    <p>{{ slugify }}</p>
</div>

{% endblock %}
